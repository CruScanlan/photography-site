import * as React from 'react';

import Layout from 'components/Layout/Layout';

const NotFoundPage = () => {
  return (
    <Layout pageTitle={'404 | Cru Scanlan Photography'} pageClass="p-NotFound" padTop={true}>
        Page Not Found
    </Layout>
  )
};

export default NotFoundPage;